namespace DoAnBackend.Data
{
    public class EnumEntity
    {
        public enum RoleEnum { Admin, User }
        public enum BlogStatus { Publish, Draft, Pending }
        public enum Gender { Male, Female, Other, PreferNotToSay }
    }
}
